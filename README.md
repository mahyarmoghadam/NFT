#NFT Marketplace

##installing

`npm install`

`npm install -g truffle`

## runnig tests

`truffle compile`

`truffle test`

## Deploy on local Blockchain

`truffle migrate`

## Open user interface:

`npm start`
